"""后台运行时配置层:让管理员在界面上覆盖 .env 的运营配置(模型/ASR/抓取等)。

模块划分:
- ``secret_box``   —— 密钥类配置的 Fernet 加解密(明文绝不落库)。
- ``config_registry`` —— 可覆盖字段白名单 + 元数据(分组/类型/是否密钥)。
- ``runtime_config`` —— 读/写/清除覆盖项,并就地刷新 ``get_settings()`` 单例。
"""
