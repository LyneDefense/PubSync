# Domain used by the standalone PubSync nginx.
DOMAIN=pubsync.example.com

# Host port exposed by the standalone PubSync nginx.
# Use 80 for a dedicated server. If another service already owns port 80,
# set this to an unused local port such as 8081 and put PubSync behind
# your server-level reverse proxy.
NGINX_HTTP_PORT=80

# Frontend base path for Vite builds. Keep "/" for an independent domain.
FRONTEND_BASE_PATH=/

DB_NAME=pubsync
DB_USER=pubsync
DB_PASSWORD=change_me

PUBLISH_TIME_HOUR=8
PUBLISH_TIME_MINUTE=0

# WeChat Official Account credentials.
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# Same-origin production access via /api usually does not need CORS,
# but keep the domain here for local admin/debug access.
CORS_ORIGINS=https://pubsync.example.com
